<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.adam.app.demoset.tablelayout.viewmodel.TicTacToeViewModel" />
        <variable
            name="activity"
            type="com.adam.app.demoset.tablelayout.DemoTableAct" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground">

        <androidx.core.widget.NestedScrollView
            android:id="@+id/scroll_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:fillViewport="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/btn_exit">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="24dp">

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/card_demo_info"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:cardBackgroundColor="?attr/colorSurface"
                    app:cardCornerRadius="12dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:strokeColor="?attr/colorOutlineVariant"
                    app:strokeWidth="1dp">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:padding="16dp">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/demo_tablelayout_instruction_title"
                            android:textColor="?attr/colorPrimary"
                            android:textSize="16sp"
                            android:textStyle="bold" />

                        <TextView
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:text="@string/demo_tablelayout_instruction"
                            android:textColor="?attr/colorOnSurfaceVariant"
                            android:textSize="13sp"
                            android:lineSpacingExtra="2dp" />
                    </LinearLayout>
                </com.google.android.material.card.MaterialCardView>

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/card_status"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    app:cardBackgroundColor="?attr/colorPrimaryContainer"
                    app:cardCornerRadius="16dp"
                    app:layout_constraintTop_toBottomOf="@id/card_demo_info">

                    <TextView
                        android:id="@+id/tv_game_status"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:gravity="center"
                        android:padding="16dp"
                        android:text="@{viewModel.messageLiveData}"
                        android:textColor="?attr/colorOnPrimaryContainer"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        tools:text="@string/demo_tablelayout_status_message" />
                </com.google.android.material.card.MaterialCardView>

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/card_board"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:cardBackgroundColor="?attr/colorSurfaceVariant"
                    app:cardCornerRadius="20dp"
                    app:cardElevation="8dp"
                    app:layout_constraintTop_toBottomOf="@id/card_status"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent">

                    <TableLayout
                        android:id="@+id/table_board"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:padding="12dp">

                        <TableRow android:layout_marginBottom="8dp">
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(0)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{0}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(1)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{1}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(2)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{2}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                        </TableRow>

                        <TableRow android:layout_marginBottom="8dp">
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(3)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{3}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(4)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{4}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(5)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{5}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                        </TableRow>

                        <TableRow>
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(6)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{6}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(7)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{7}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                            <Button
                                style="@style/Widget.Material3.Button.TonalButton"
                                android:layout_width="90dp"
                                android:layout_height="90dp"
                                android:layout_margin="4dp"
                                android:onClick="@{() -> viewModel.playerMove(8)}"
                                android:textSize="32sp"
                                app:board="@{viewModel.boardLiveData}"
                                app:cellIndex="@{8}"
                                app:winningCells="@{viewModel.winningCellsLiveData}" />
                        </TableRow>
                    </TableLayout>
                </com.google.android.material.card.MaterialCardView>

            </androidx.constraintlayout.widget.ConstraintLayout>
        </androidx.core.widget.NestedScrollView>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_exit"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:onClick="@{() -> activity.finish()}"
            android:text="@string/label_exit"
            android:textColor="?attr/colorError"
            android:textSize="18sp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:background="?android:attr/colorBackground"
            android:elevation="4dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>